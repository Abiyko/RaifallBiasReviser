Python3での使用を想定しています。

プロジェクトのルートディレクトリからセットアップファイル

Windows -> setup.bat

Mac,Linux -> setup.sh

を実行し、生成されたファイル(run.bat, run.sh)を実行してください。

水門水質データベースの特定の降雨観測点と同じ座標のd4PDFの降雨データをバイアス補正します。

入力データはテキストファイルです。

タイムステップは一致させてください。

同じ地点のファイル名は、先頭に同じ名前を付け、アンダーバー(_)で区切ってください。

例: Awadu_m001_smqr.txt
